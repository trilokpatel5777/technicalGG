
##############################################

docker --help |less
docker container ls          docker parent-command child-command
docker image ls
docker network ls
docker container run ubuntu sleep 30
docker container ls -a      to list all the containers
docker container run nginx
docker container rm id (we can give 2-3 starting characters of the id) we can also remove 2 at the same time
docker container start nginx
docker container stop nginx
docker container stop cont-id
docker container run -d ubuntu sleep 30  (detach)

docker container run -it ubuntu /bin/bash   (interaactive tty)
control+p+q  bahar nikal na ho container se and container bhi chalu rahega

docker container run -d nginx
docker container inspect cont-id      to get the details of the container
docker container logs cont-id 
docker container top cont-id          to get the running processes
docker container stats                to see processes of all the running container

portmapping video 8 
docker container run -d -p 3600:80 --name test1_web nginx       3600- local machine port 80-nginx container port
netstat -nltp to check which ports are listening
check the machine ip:3600 in the browser and nginx page will come

docker container run exec -it cont-id /bin/bash          to get bash terminal
docker container rename  cont-id tiger_web
docker container restart cont-id

docker container run -d nginx
docker container attach cont-id 
docker container run -itd ubuntu  /bin/bash
docker container attach cont-id

docker container kill cont-id                                  will kill the container
docker container stop cont-id

docker container wait cont-id                                  wait till the container stops
docker container pause cont-id                                 will pause all the processes of that container
docker container unpause cont-id                               will unpause all the processes of that container
docker container prune   or prune -f                           will delete all the containers that are not in use
docker container port cont-id                                  gives details of port mapping

docker container create ubuntu sleep 60                        sleep 60 can be any command which we want during creation - will not start the container, run command will create and start the container as well
docker container start ubuntu
docker image ls                                                lists allthe image
docker container diff cont-id                                  to see the changes from actual image -- C-create A-add D-delete
docker container run -it ubuntu /bin/bash
watch 'docker container diff cont-id'                          to monitor continously in every 2 sec-watch command is used 
docker container cp /test /cont-id:/mnt                        to copy /test folder from machine to container /mnt folder

docker container run -it ubuntu /bin/bash                      made some changes to the container and ctl+p+q
docker container export ubuntu > my_ubuntu_tree_git.tar        will get the tar file of the container
docker container export ubuntu -o my_ubuntu_tree_git.tar        will get the tar file of the container   2 ways
docker image import my_ubuntu_tree_git.tar my_ubuntu_NEW        my_ubuntu_NEW is the name and it can be any
docker image ls
docker container run -it my_ubuntu_NEW /bin/bash


docker container run -it ubuntu /bin/bash 
docker container commit --author *Trilok Patel* -m "This is test commit" Cont-id my_test_image(image name)
docker container rm -f ubuntu
docker container run -it my_test_image

docker image (hub.docker.com -- will get all the images)
docker pull ubuntu:14.04                                        to pull any image 
docker image ls
docker image tag my_ubuntu_NEW hub.docker.com/gauravsharma6421/my_ubuntu123    
docker login 
docker push gauravsharma6421/my_ubuntu123   
docker pull gauravsharma6421/my_ubuntu123









 


